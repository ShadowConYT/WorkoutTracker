import React, { useEffect, useState } from 'react';
import {Stack, Box, Typography} from '@mui/material';
const wwd = () => {
  return (
    <Stack>
        <Typography color={'whitesmoke'} fontWeight={700} sx={{ fontSize: { lg: '44px', xs: '30px' } }} mb="49px" textAlign="center">
            What We Do ?
        </Typography>
    </Stack>
  );
};

export default wwd;